<UserControl x:Class="ServiceApplication.MVVM.Controls.DateTimeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ServiceApplication.MVVM.Controls"
             mc:Ignorable="d" 
             >
    
    <!-- DateTime Section -->
    <StackPanel HorizontalAlignment="Center" >
        <!-- Binding till datum som ska uppdateras automatiskt -->
        <TextBlock Text="{Binding CurrentTime, FallbackValue=--:--}" 
                   Foreground="#f5f5f5" 
                   HorizontalAlignment="Center" 
                   FontSize="92" 
                   Margin="0 -25 0 0"/>
        <TextBlock Text="{Binding CurrentDate}" 
                   Foreground="#aaaaaa" 
                   HorizontalAlignment="Center" 
                   FontSize="22"/>
    </StackPanel>

</UserControl>
